# Photo viewer repair — Astra review

User explicitly authorized Astra takeover after local Spark failed initial attempt and two corrections. S3 remains rejected: task20260907-153712-ce1f11 durations303.88s/206.73s/603.77s, local generated11832/9209/25562 tokens. Final partial snapshot changed only parser, lacked success return and viewer gate, and failed native immutable compilation (verify-747084fd). No Spark output was applied. F3 is the separately authorized Astra implementation route, not Spark success or a pure-frontier comparison.

Astra implemented the two functions directly against unchanged original hashes. append_photo_caption now validates exactly one matching complete six-field row before creating a caption. Missing/read-error/blank/control-character/extra-field/truncated/duplicate matching rows reject. CRLF and complete final line without newline work. All unrelated rows may remain independent. No species is inferred from article title; valid caption text and hazard coloring unchanged. show_photo_viewer_screen creates/decodes an image only after caption success; otherwise explicit missing-identification message appears, with Prev/Next retained. clear_body still precedes every transition; valid-image decoder/ownership path unchanged. Attribution is shown only with valid metadata. This is syntax/mapping protection, not validation of biological correctness or file authenticity.

Parent regression executes the actual production function bodies with minimal observable LVGL and file-read substitutes. Original baseline compiled then failed the missing-metadata image assertion. Final scenarios pass, including switching valid->invalid->valid and hazard image navigation; expanded malformed/control/truncated cases pass. Mutation removing the viewer gate makes the test fail. Structural comparison proves content outside the two authorized functions remains byte-identical. Changed function formatting checked; unrelated pre-existing formatting preserved.

Both full target builds succeeded using official IDF5.5.4 container before repair; both incremental TFT and AMOLED builds succeeded with the actual repaired source. dependency lock unchanged. Simulator71/71PASS; UI boundary check and git whitespace check PASS. No physical display/SD fault tests or flash performed. GitNexus impact caption LOW/1viewer caller, viewer UNKNOWN (UI callback references confirmed manually). Refreshed detect-changes reports caption plus prior3NMEA symbols,LOW,no enumerated flows, but fails to map viewer body edits; actual diff/scope comparison confirms viewer as the fifth changed production symbol. Index incompleteness is not an absence-of-impact claim.

CI now runs scripts/check_field_guide_photo_safety.py and includes its/script source path filters. Source/test/workflow changes remain uncommitted. S4 subsequently timed out without edits despite output16384/context32768 and deadline900s. Astra completed the staging repair under the photo takeover; ten tests pass. See PHOTO-STAGING-REVIEW.md. Team-key and nonce fixes remain open; see SECURITY-IMPLEMENTATION-PLAN.md.
