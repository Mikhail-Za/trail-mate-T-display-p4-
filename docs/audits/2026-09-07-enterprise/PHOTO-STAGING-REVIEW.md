# Photo staging repair — Astra review

S4 local task20260907-160021-7edb37 timed out903.96seconds,35446local generated tokens, no changes/tests. Actual resolved config output16384/context32768 proves the increased allowance was used. User had explicitly selected Astra to finish the photo repair; F4 is the separately measured takeover, not Spark success. No Spark patch integrated.

Actual original main() was exercised with temporary numeric source0/1 photos and stubbed image I/O: outputs renumbered1/2 while credits remained0/1. This reproduces the mapping defect without touching real photos/cards. Final implementation extracts only photo work into stage_photos(raw_root, photos_dir), preflights all decks, then renumbers image and corresponding credits together using unique source numeric identities. Documented JPG credit names for PNG sources remain supported. Missing/extra/duplicate/blank/incomplete/control-character metadata, duplicate numeric images, non-file image sources, more than6photos and source symlinks fail before photo output writes. Species/artist/license/URL stay unchanged. Existing baseline JPEG/500px/quality80 behavior retained. Unrelated guide/font/translation logic unchanged.

Ten host tests PASS with observable image I/O substitutes, including main() using the validated helper. Independent real Pillow12.1.0 check converted progressive JPEG and alpha PNG into baseline RGB JPEG with expected dimensions; correct deadly-lookalike and ordinary captions retained after renumbering. Python compile, both photo checks, Ruff0.14format of new checks and git whitespace checks pass. No image-metadata content correctness/biological validation or atomic/power-loss staging guarantee claimed. Copy only after STAGING_DONE; generated photo output can be partial after a later image I/O failure. Actual photos_raw collection/cards are unavailable here, so their validation remains a deployment check.

CI runs both photo checks and includes their source paths. Source instructions and hardware bench checklist updated. Runtime viewer patch separately passed both TFT/AMOLED IDF5.5.4 builds and71simulator tests; Python staging changes do not alter firmware. No flash/push. Team and nonce security work remains open; new packet allocator taskS5 is independent and does not itself complete the nonce repair.
