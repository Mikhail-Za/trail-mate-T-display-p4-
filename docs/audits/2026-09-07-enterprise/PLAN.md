Trail Mate audit plan — baseline e3c0fbd; clean checkout.
User authorizes full enterprise-level audit, bug/security fixes, updates, optimization recommendations; explicitly requests parallel Anthropic Sonnet workers, Ponytail, Crucible and Conductor. Astra coordinates and verifies. Sonnet overrides default GPT workers; native Claude OAuth only, no billing/auth/provider changes. Crucible remains Fable per explicit review policy.
Observed: offline ESP-IDF multi-target firmware, P4 TFT primary, AMOLED secondary; Linux host tests; C6 companion and SD content pipeline. MAINTAINERS July 11 supersedes earlier hardware state. Hardware availability now unverified; never claim hardware tests. Shared memory has no project entry. GitNexus MCP absent, local index absent, referenced skill absent; symbol edits blocked until impact tooling available. No installed IDF found at standard home locations.
Phase A after this plan gate: three independent read-only Sonnet Claude CLI workers with pinned model and restricted Read/Glob/Grep tools, no recursive delegation. Each confirms reading Ponytail. Assign security (mesh/team crypto, parsers, C6 trust boundaries); runtime (maps/GPS, storage, concurrency/LVGL); delivery (build/CI, pinned dependencies, content pipeline, maintainability). Each returns concrete file/line evidence, trigger, severity, proposed minimal fix, failable check, inspected coverage and gaps. No speculative CVE claims or exhaustive-certification claim. Vendor code inventoried, focused review at interfaces.
Astra in parallel determines available GitNexus CLI and host build/test tools, runs baseline supported tests. No installations or global configuration changes solely to make review work. Dependency advisories may be checked read-only against official sources, at most five external fetch/search calls; updates only when compatibility and tests establish benefit, otherwise version-specific recommendations.
Phase B: consolidate and independently reproduce load-bearing findings. Before each symbol edit run GitNexus upstream impact and report callers/processes/risk; warn HIGH/CRITICAL. If tooling unavailable retain patches as recommendations without bypassing mandatory gate. For fixes with available impact/build verification create bounded Sonnet implementation assignments in separate worktrees, no overlapping writers. No blind dependency bump, protocol redesign, medical-content change, flash/deploy or SD overwrite. Existing safety-critical captions preserved. Any change needing protocol/config/hardware decision is presented as concrete recommendation.
Acceptance: report covers all three audit domains with explicit coverage/gaps; findings trace to actual baseline and reachable triggers; each applied substantive fix has a failing-before/passing-after regression check where feasible; Astra reads actual diffs and reruns checks; TFT compile required before firmware commits, AMOLED when affected; config regeneration/diff only for authorized config changes with build support. GitNexus detect_changes required before commits. No push or deploy. If missing toolchain, clearly label firmware unverified and deliver proposed firmware changes as written recommendations only; no unverified firmware edits in the main checkout. Final fresh Crucible review against report/diff and check evidence, resolve material findings then independently verify. Report fixed vs confirmed-open vs unverified candidates separately, prioritized update/optimization recommendations, exact commands/results, hardware handoff checks. No performance gain claim without measurement.
Review gates each max five rounds; unavailable reviewer or transport/auth failure stops per skill, only confirmed Fable quota exhaustion permits fresh Opus 5 fallback.

Round 1 clarifications:
- Verified baseline: branch main; full HEAD e3c0fbd68663bbeef09056a22cbce950065304bc; git status --porcelain empty; git merge-base --is-ancestor 98609d5 HEAD exit 0. Current checkout is explicitly user-selected; do not switch it based on stale manual branch names.
- Astra performs the Conductor/orchestrator role; Conductor is a workflow, not a separate required executable. User's Sonnet selection overrides GPT worker defaults. Exact worker model claude-sonnet-4-6; no fallback or GPT workers. Each loads Ponytail and receives required docs AGENTS.md, MAINTAINERS.md, BENCH_TEST_CHECKLIST.md, MAP_UX_ROADMAP.md.
- Known items to seed: Team plaintext PSK and NVS persistence, blocking walkie TX, older cross-target compile breaks, map follow-ups, corrupt LASTFIX nonfinite-input history. Verify current code before classifying as previously known/open. SD parser scope explicitly includes index.tsv, regions.tsv, CREDITS.tsv, waypoints.tsv, LASTFIX.DAT, binfonts, translate TSVs. External boot launcher and its partition redesign are outside scope.
- Reproduction classifications: independently code-traced (trigger to sink with lines), host-runtime reproduced (actual compiled target/test), hardware reproduced (only if actually run; none currently available). Do not conflate them. Configure with compile_commands export for host membership checks.
- Tool decision: cached/local GitNexus searches found no CLI/index; IDF absent at normal locations. Repo-local indexing is permitted if an existing CLI becomes available, never commit generated index. No tool installs/global changes for this audit. Expected outcome for symbol fixes is recommendations only; no implementation workers/worktrees without impact tooling. Tiny non-symbol build-source-list fixes can be done inline by Astra, host-verified, saved as diff; no firmware verification or commit claimed. This preserves authorized useful work despite environment limitations.
- Host baseline: Ninja preset cannot configure (missing Ninja); existing Unix Makefiles generator with simulator ON, GTK/Cardputer OFF configured successfully. 69 registered tests; 68 pass, phone_core_smoke cannot link due to missing channel_hash.cpp implementation. Build keep-going allowed collection of all other results. No firmware build claimed.
- Do not run content pipeline without explicit temporary staging and existing deps. Worker reports and consolidated AUDIT.md, checks, proposed changes and review artifacts live in /tmp/trailmate-audit-20260907; final deliverable may be copied to docs/audits with final review log links.
