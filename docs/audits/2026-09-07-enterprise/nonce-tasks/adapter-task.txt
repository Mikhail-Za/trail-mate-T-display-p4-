Apply Ponytail from PONYTAIL.md and acknowledge it in final handoff. You are not alone: preserve other work; no delegation. Parent Astra has reviewed plan and upstream impact. Only modify platform/esp/radio/meshtastic_radio_adapter.cpp and .h. Only code changes: sendText, applyConfig, sendEncodedPayload; include new storage header; remove next_packet_id_ member; add optional chat::MessageId* out_msg_id=nullptr to private sendEncodedPayload declaration and matching definition (last argument). All other methods byte-identical. Tests and storage header immutable.

Goal: ONE durable allocation in sendEncodedPayload for every send, no double allocation for text.
1. Include platform/esp/radio/persistent_packet_ids.h in cpp. In applyConfig, after copying config_, call initializePacketIds(node_id_,config_). Log error if false; keep updateChannelKeys and configureRadio so reception remains possible. Storage owner rejects all sends itself after initialization failure. No resets on repeated applyConfig.
2. In sendText retain out_msg_id=0, ready/empty and encoding checks. Remove next_packet_id_ allocation and old out assignment. encodeTextMessage's unused packet-ID argument can be0 (parent verified encoder); then return sendEncodedPayload(... peer,false,0,true,out_msg_id).
3. In sendEncodedPayload clear optional out_msg_id initially. Retain existing ready/payload/len/board checks. Before reading channel key or allocating, cast channel to size_t, reject >=chat::kMaxChannels or !channel_enabled_[index]. This avoids a disabled/invalid channel falling through to unencrypted transmission. Get psk/hash/destination as before. Replace old requested-or-increment expression with chat::MessageId msg_id=0; allocatePacketId(node_id_,psk,psk_len,packet_id,msg_id); on false log non-secret explanation (legacy keys require replacement or persistent counter unavailable) and return false. On success write optional out pointer before buildWirePacket/transmit so failed TX retains its assigned ID. Keep wire codec, transmit result events, RX rearm and logs unchanged. Failure consumes reserved ID; caller cannot reuse it.
4. Delete next_packet_id_ from header. Other sendEncodedPayload callers keep their existing7 args using default null pointer. No protocol, crypto, provider, caller, scheduler or decoder changes.

Run python3 scripts/check_radio_packet_ids.py. It compiles actual changed function bodies plus the real wire codec, with observable persistent allocator and AES/board substitutes. Original compiles then fails missing persistent-initialization/allocation assertion. Check default/explicit IDs, repeated explicit rejection, text output ID, failed TX burning ID, disabled/invalid channels, allocation failure, applyConfig and second adapter sharing sequence, and actual AES nonce inputs. Full firmware compilation is parent responsibility. Missing board/nanopb headers in this minimal stage are expected; use the supplied check.

Final handoff: paths/functions, test command/result, scope confirmation and Ponytail acknowledgement.
