Targeted correction of the SAME task. The prior attempt spent its deadline reading without delivering an implementation. Implement now. Do not repeat repository discovery or reread entire tests; all required details are below. Your first productive action should write platform/esp/radio/persistent_packet_ids.cpp. Read PONYTAIL.md once if needed and acknowledge it. Only cpp is production scope.

Parent-authored test correction authorized: in scripts/check_packet_id_storage.py replace the init_sha fixture `cfg.channels[0].key_len=16;fail_sha=true;` with `cfg.channels[0].key_len=16;cfg.channels[0].key[0]=0xA5;fail_sha=true;`. This is the ONLY permitted test change. All-zero bytes mean absent key under the actual normalize helper, so the old fixture could not trigger SHA. Retain assertions unchanged. Parent recorded this discovery before your result; it is not a relaxation.

Exact APIs already declared in supplied header: namespace platform::esp::radio, bool initializePacketIds(uint32_t node,const chat::MeshConfig& config); bool allocatePacketId(uint32_t node,const uint8_t* psk,size_t psk_len,uint32_t requested,uint32_t& out). Include own header, chat/domain/persistent_packet_id_allocator.h, nvs.h, mbedtls/sha256.h, array/mutex/optional/cstring. No esp_log/esp_err includes (test NVS stub already declares errors/types).

Anonymous namespace static mutex, initialized=false, failed=false, node value, array<uint8_t,273> record, optional<chat::PersistentPacketIdAllocator>. Lock both APIs through NVS calls. Fixed blob TPI1 bytes0..3, node LE32 at4, bound LE64 at8, count at16, SHA256 fingerprints at17 (8 slots of32). Validate exact273 bytes, magic, node, bound1..2^32, count<=8 and zero unused fingerprint bytes. Use helper LE loops, no struct reinterpretation.

Init: failed =>false; if already initialized return node matches (NEVER reset/reload). nvs_open(tm_pkt_ids,NVS_READWRITE); query nvs_get_blob(state_v1,nullptr,&size); only NOT_FOUND allows create, all other errors latch failed. Existing: read exact size, check actual size and validate, close; allocator.emplace(bound), set initialized. Missing: zero record, set metadata/bound1; for8 channel slots regardless enabled, normalizeMeshtasticChannelKeyLen(rec.key,sizeof(rec.key),rec.key_len); hash when len16/32 using checked mbedtls_sha256(key,len,out,0). Append fingerprint; duplicates allowed. Write set_blob then commit, close; only on success initialize allocator. NVS or SHA errors always latch failed; malformed existing blob never recreated.

Allocate: out=0; require initialized,!failed,node match; reject lengths outside0/16/32 and null nonzero key. Hash actual expanded PSK with checked mbedtls_sha256; compare legacy fingerprints and reject matches without setting failed. On SHA error set failed. Call allocator.allocate(requested,callback,out). Callback copies record, updates bound, opens NVS READWRITE, set_blob state_v1 and commit, closes; update in-memory record only on success. Storage error sets failed. Reserve-before-use, same allocator across threads/adapters, no per-packet NVS write within1024reserved range, no wrap. Failed callback cannot return an ID and cannot be retried this boot. All NVS handles closed, no raw keys persisted/logged.

Run python3 scripts/check_packet_id_storage.py after writing cpp (23process scenarios). Final handoff with test result, paths, Ponytail acknowledgement. No further design discussion until a file exists.
