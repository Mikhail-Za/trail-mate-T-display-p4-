Implement platform/esp/radio/persistent_packet_ids.cpp ONLY. Do not edit frozen headers/tests. You are the local Tiel worker; Astra has reviewed the plan and user authorizes implementation. Read PONYTAIL.md and explicitly acknowledge applying it in handoff. You are not alone: preserve other work, no delegation.

Required API is in persistent_packet_ids.h. Reuse chat::PersistentPacketIdAllocator (header supplied). Use a single process-wide anonymous-namespace owner guarded by std::mutex in BOTH functions; no constructible public stores/reset APIs. Hold lock across allocation and durable commit. State includes initialized, failed (latched), node, persisted blob and allocator (std::optional works).

NVS namespace tm_pkt_ids, key state_v1. Fixed 273 bytes: magic TPI1 at0, node little endian32 at4, high-water LE64 at8, count at16, eight SHA256 fingerprints at17 (32 each). Unused fingerprint bytes zero. Bound valid1..2^32 inclusive. Validate exact size/magic/node/bound/count<=8/zero unused bytes. Treat only ESP_ERR_NVS_NOT_FOUND from get_blob as new state; other errors latch failed. Use nvs_open READWRITE, nvs_get_blob size query then exact read, close on every path. Do not clear/erase NVS.

First initialize when missing: capture normalized16/32-byte keys from ALL config.channels including disabled. normalizeMeshtasticChannelKeyLen from chat_types.h is authoritative; hash original key bytes with mbedtls_sha256(key,len,out,0), check return0. Skip normalized0/1 public/no keys. count<=8 (duplicates okay). Set node/highwater1, commit blob before initialized. Existing record loads allocator from stored highwater (skips entire previous reservation). Repeated initialization same node cannot reload/reset; different node rejected. Any NVS or hash error latches failed until reboot, including uncertain commit. Invalid existing record NEVER recreated.

allocate: out=0 initially; require initialized and !failed and correct node. len must0,16,32 and nonnull for nonzero. Hash expanded current PSK and compare against each captured fingerprint; deny a legacy match (without latching permanent failure). Hash failure latches failure. Then allocator.allocate(requested,reserveCallback,out). Callback copies full blob, updates bound, nvs_open/set_blob/commit/close; updates in-memory blob only on full success; all storage errors latch failed. Global allocator ensures no duplicate IDs with threads or two same-node clients. Reject consumed explicit IDs/exhaustion, never reset on key change, never issue IDs if commit fails. No raw-key logging, no logging dependency, no heap new stores per call, no model/provider changes.

Acceptance: python3 scripts/check_packet_id_storage.py (23 fresh-process scenarios; real production cpp with observable NVS/SHA substitutes). Compile using existing g++/bfd through the script. Tests are immutable. Implement promptly, then run check. Final handoff must include changed path, test command/result, limitations, Ponytail acknowledgement. Real ESP-IDF builds are parent responsibility.
